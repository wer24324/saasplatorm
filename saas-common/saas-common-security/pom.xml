<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.audit</groupId>
        <artifactId>saas-common</artifactId>
        <version>2.5.0</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>saas-common-security</artifactId>

    <description>
        audit-common-security安全模块
    </description>

    <dependencies>

        <!-- Spring Web -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
        </dependency>

        <!-- audit Api System -->
        <dependency>
            <groupId>com.audit</groupId>
            <artifactId>saas-api-system</artifactId>
        </dependency>

        <!-- audit Api Tenant -->
        <dependency>
            <groupId>com.audit</groupId>
            <artifactId>saas-api-tenant</artifactId>
        </dependency>

        <!-- audit Common Redis-->
        <dependency>
            <groupId>com.audit</groupId>
            <artifactId>saas-common-redis</artifactId>
        </dependency>

    </dependencies>

</project>
